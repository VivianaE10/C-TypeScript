function imprimeMensage(message: string): void {
  console.log(message);
}

imprimeMensage("hola, soy muy buena programando");

function sumar(num1: number, num2: number): number {
  return num1 + num2;
}

let resultado: number = sumar(5, 10);

console.log(`Tu resultado es:  ${resultado}`);
